<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wasatch Bitworks</title>

     <link rel="icon" type="image/x-icon" href="/favicon.ico">

    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Optional: Favicon -->
    <!-- <link rel="icon" href="/images/favicon.ico" type="image/x-icon"> -->
  </head>

  <body class="bg-gray-900 min-h-screen flex flex-col">

    <!-- Header / Navigation -->
    <header class="w-full">
      <nav class="flex items-center justify-between p-6 lg:px-8">
        <div class="flex items-center">
          <a href="/" class="block">
            <img class="h-[120px] w-auto" src="/images/new_bitworks_tsp.png" alt="Wasatch Bitworks Logo">
          </a>
        </div>
        <div class="flex flex-col lg:flex-row gap-4 lg:gap-x-8 items-center">
          <a href="/" class="text-sm font-semibold text-white">Home</a>
          <a href="/services" class="text-sm font-semibold text-white">Services</a>
          <a href="/about" class="text-sm font-semibold text-white">About</a>
          <a href="/contact" class="text-sm font-semibold text-white">Contact</a>
        </div>
      </nav>
    </header>

    <% if session[:error] %>
      <div class="mb-4 rounded-lg bg-red-100 border border-red-400 text-red-700 px-4 py-3">
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>

    <% if session[:success] %>
      <div class="mb-4 rounded-lg bg-green-100 border border-green-400 text-green-700 px-4 py-3">
        <p><%= session.delete(:success) %></p>
      </div>
    <% end %>

    <!-- Main Content (page-specific content gets inserted here) -->
    <main class="flex-grow flex flex-col">
      <%== yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800">
      <div class="max-w-7xl mx-auto py-4 px-6 text-center text-sm text-gray-400">
        &copy; <%= Time.now.year %> Wasatch Bitworks LLC. All rights reserved.
      </div>
    </footer>

  </body>
</html>