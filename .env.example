# Wasatch Bitworks - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Mailgun SMTP Configuration
# Get this from: https://app.mailgun.com/app/sending/domains
# Navigate to: Domain Settings > SMTP Credentials
MAILGUN_SMTP_PASSWORD=your_mailgun_smtp_password_here

# Optional: Session Secret (will auto-generate if not set)
# SESSION_SECRET=your_random_secret_key_here

# Optional: Rack/Sinatra Environment
# RACK_ENV=development
